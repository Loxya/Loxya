/* eslint-disable import/prefer-default-export */

/**
 * Les langues prises en charge avec la région
 * par défaut utilisée pour chacune d'elles.
 */
export const AVAILABLE_LANGUAGES: Readonly<Record<string, string>> = Object.freeze({
    fr: 'fr-FR',
    en: 'en-GB',
});
