import common from './common.yml';
import date from './date.yml';
import errors from './errors.yml';
import legals from './legals.yml';

export default {
    ...common,
    ...date,
    ...legals,
    errors,
};
