/** L'orientation de la navigation multi-étapes. */
enum StepperOrientation {
    /**
     * Étapes empilées verticalement.
     * (mode par défaut, sidebar)
     */
    VERTICAL = 'vertical',

    /** Étapes alignées horizontalement. */
    HORIZONTAL = 'horizontal',
}

export default StepperOrientation;
